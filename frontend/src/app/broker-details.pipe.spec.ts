import { BrokerDetailsPipe } from './broker-details.pipe';

describe('BrokerDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new BrokerDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
